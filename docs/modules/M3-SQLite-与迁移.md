# M3 — SQLite 与迁移

## 状态

**Planned**

## 目标

建立 Standalone 权威元数据源并安全导入旧 localStorage/MySQL 数据。

## 非目标

- 不提前实现后续模块。
- 不改变 YOHO 算法语义或宣称算法原创。
- 不加入未经验证的指标、部署能力或临床结论。
- 不覆盖 `F:/YOHO-Manager` 原项目及其未提交修改。

## 前置依赖

- M2 已完成并提供交接记录。
- 已阅读根 `README.md` 与 `docs/contracts/README.md`。

## 允许修改的目录

- Repository/SQLite、迁移工具与数据层测试。
- 本文件及 `CHANGELOG.md` 的当前模块记录。

## 禁止触碰的目录

- `F:/YOHO-Manager/**` 默认只读。
- 其他模块内部实现；如需跨模块变更，先更新 contracts 和影响说明。
- 任何真实患者数据、无明确许可的数据集或凭据文件。

## 输入与输出

### 输入

- 上一模块已冻结的契约、测试与交接记录。
- 当前模块批准的需求和脱敏 fixture。

### 输出

- 当前模块实现及自动化测试。
- 新增/变更契约、示例、错误码和版本说明。
- 测试命令及真实结果。
- 完整的下一模块交接信息。

## API / Event / Error Contract

- 当前状态：待本模块实施时填写。
- 不得自行设计与 `packages/contracts` 平行的接口。
- 破坏性变化必须先版本化并补充迁移/兼容策略。

## 数据库或文件变更

- 当前状态：待本模块实施时填写。
- 所有变更必须说明迁移、回退、数据保护及 artifact 影响。

## 实现步骤

1. 阅读根文档、contracts 与上一模块交接。
2. 检查 Git 状态和允许修改范围。
3. 先补充/复用契约及契约测试。
4. 在模块边界内实现最小完整功能。
5. 运行单元、集成、契约和必要的真实应用验证。
6. 更新本文件、CHANGELOG 和交接记录后停止。

## 测试与验收命令

- 当前状态：尚未实施。
- 完成时必须记录实际执行命令、环境、通过/失败数量和未执行原因。
- 不得以手工观察代替可自动化的测试。

## 已知限制

- 当前仅有规划骨架，具体限制须在实施过程中如实更新。

## 交接给下一模块

下一模块：**M4 Python Worker 与评测**。

交接前必须填写：

- 完成项与未完成项；
- 修改文件；
- 契约版本及冻结点；
- 可直接复用的 API/类型/fixture；
- 数据迁移状态；
- 测试结果；
- 已知风险；
- 下一模块第一步；
- 明确禁止破坏的行为。

## 变更记录

- 2026-09-06：创建模块计划骨架。
